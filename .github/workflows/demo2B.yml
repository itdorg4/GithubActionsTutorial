name: Sequential Workflow

on: [push]

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: First Job
        run: echo "🚀 Running Job 1"

  job2:
    needs: job1
    runs-on: ubuntu-latest
    steps:
      - name: Second Job
        run: echo "⏭️ Running Job 2 (after Job 1)"

  job3:
    needs: job2
    runs-on: ubuntu-latest
    steps:
      - name: Third Job
        run: echo "✅ Running Job 3 (after Job 2)"